const ROOTS = {
    AUTH: '/auth',
    DASHBOARD: '/',
    ROOT: '/dashboard',
}

// ----------------------------------------------------------------------

export const paths = {
    // AUTH
    auth: {
        login: `${ROOTS.AUTH}/sign-in`,
    },

    // DASHBOARD
    dashboard: {
        root: ROOTS.DASHBOARD,
    },
}
